---
id: "M4.5"
title: "Chunk loading and automatic resume"
state: "PLANNED"
milestone: "M4"
depends_on:
  explicit:
    - task: "M4.4"
      reason: "Approved addition depends on source-block job state and cancellation."
  inferred: []
risk: "critical"
sensor_profiles:
  - "default"
  - "architecture_sensitive"
  - "runtime_minecraft"
plan_reference:
  document: "docs/M4_JOBS_AND_CHUNK_LOADING.md"
  sections:
    - "Tickets (M4.5)"
    - "Save/restart"
objective: "Keep nine ticking chunks while mining and one while idle; safely restore and resume saved running jobs."
allowed_scope:
  - "Worker-owned NeoForge ticket controllers, UUID ownership and lifecycle cleanup."
  - "Persist worker inventory/identity and product job state needed for automatic resume."
  - "Update M5-M6 specifications for cross-dimension control and restart semantics."
forbidden_scope:
  - "No duplicate scanner/pathfinder/mining engine, saved path internals or offline catch-up."
  - "No GUI implementation, configurable radii, storage automation or dashboards."
acceptance_criteria:
  - id: "AC-1"
    statement: "Nine explicit ticking tickets while mining and one while idle follow the worker without leaking across overlap/removal/dimension changes."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Focused runtime ticket lifecycle checks."
  - id: "AC-2"
    statement: "Real save/reload restores inventory and resumes only saved RUNNING jobs toward remaining source-block counts with fresh runtimes."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Two-process server restart evidence and persisted state assertions."
  - id: "AC-3"
    statement: "Missing workers release restored tickets after entity loading; invalid saved jobs fail without retry loops."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Orphan cleanup and invalid-state cases."
blocker_conditions:
  - "Chunk ownership or restart behavior requires a duplicate native engine."
  - "Tickets leak or a cancelled/completed job resumes."
---

Use docs/M4_JOBS_AND_CHUNK_LOADING.md as the approved implementation contract.
