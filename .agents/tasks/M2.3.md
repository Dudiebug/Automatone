---
id: "M2.3"
title: "Make direct server-side movement reliable"
state: "PLANNED"
milestone: "M2"
depends_on:
  explicit: []
  inferred:
    - task: "M2.2"
      reason: "INFERRED scheduling edge from the plan's Recommended implementation order; the plan states no explicit task-level dependency."
risk: "high"
sensor_profiles:
  - "default"
  - "architecture_sensitive"
  - "runtime_minecraft"
plan_reference:
  document: "docs/NEOFORGE_1.21.1_SERVER_WORKER_MILESTONES.md"
  sections:
    - "284-304: Milestone 2 goal, required modules, and integration replacements"
    - "327-335: M2.3 — Make direct server-side movement reliable"
objective: "Make native Automatone movement input control the worker deterministically on the server without packets, fake clients, jitter, or vanilla controls overwriting it."
allowed_scope:
  - "Verify Automatone forward/sideways/jump/look input moves the custom entity."
  - "Prevent vanilla goals/navigation/MoveControl/LookControl/JumpControl from overwriting active Automatone decisions."
  - "Prefer solving conflicts in WorkerEntity, not global mixins."
  - "Define inactive behavior as simply standing still."
  - "Confirm cancel clears stale movement."
forbidden_scope:
  - "Do not add a fake client, fake player, packet-driven movement, or client authority."
  - "Do not add global mob-control mixins when worker-local control can express the invariant."
  - "Do not implement custom pathfinding or movement scripts."
  - "Do not add combat, wandering, following, storage, GUI, networking, quantity, or persistence behavior."
  - "Do not begin GUI work before Milestone 3 passes."
acceptance_criteria:
  - id: "AC-1"
    statement: "Native Automatone forward, sideways, jump, and look input produces deterministic server-side worker movement."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Movement GameTest observes expected position/rotation transitions over bounded ticks."
  - id: "AC-2"
    statement: "Vanilla goals, navigation, and movement controls do not overwrite active Automatone decisions."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Conflict test runs with vanilla controls present and shows no input zeroing or jitter."
  - id: "AC-3"
    statement: "Control-conflict resolution is worker-local wherever possible and introduces no broad global mixin."
    sensor: "archunit"
    blocking: true
    evidence: "Architecture/mixin report and source review show worker-local suppression."
  - id: "AC-4"
    statement: "Inactive behavior is standing still and cancellation clears stale movement."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Idle/cancel observation window records no continued movement."
blocker_conditions:
  - "Deterministic movement requires fake-client/player input or packet authority."
  - "Vanilla conflict can be solved only with an unapproved global mob mixin."
  - "Native pathing cannot supply the movement decisions without a custom movement script."
  - "Rotation, raycast, hitbox, or step-height assumptions cannot be observed in the runtime fixture."
  - "Required runtime evidence is unavailable."
---

# M2.3 — Make direct server-side movement reliable

## Metadata

- State: PLANNED
- Milestone: M2
- Depends on: INFERRED M2.2 scheduling edge; no explicit task-level dependency
  is stated by the plan.
- Risk: high
- Sensor profiles: default, architecture_sensitive, runtime_minecraft
- Approved plan reference: plan lines 284-335.

## Objective

Make native Automatone movement input control the worker deterministically on
the server without packets, fake clients, jitter, or vanilla controls
overwriting it.

## Why this task exists

The worker is not a player and must not require fake-client input
infrastructure. It must apply the movement decisions already produced by
native Automatone while resolving control conflicts locally on the worker.

This task proves direct movement, not custom pathfinding. Native goals and
pathing remain Automatone-owned.

## Source and documentation references

| Type | Repository / source | Ref / version | Path / symbol | Why relevant |
| --- | --- | --- | --- | --- |
| plan | local approved plan | current | docs/NEOFORGE_1.21.1_SERVER_WORKER_MILESTONES.md, lines 284-304 | worker/movement architecture |
| plan | local approved plan | current | M2.3 task, lines 327-335 | exact movement checklist and done-when |
| source baseline | Dudiebug/Automatone | 545c552d6f3c333e32a256dd82227004954dd5c3 | fork 1.20 branch, Minecraft 1.20.1 | immutable source baseline |
| planning baseline | Dudiebug/Automatone | d555e41d0beb96194de5ff7c0a8dac7b701d0581 | planning baseline | immutable planning reference |
| behavioral reference | minefortress-mod/automatone | 58d090edaa3b8fb6cfdd3b1fecacf41f60ab8d4c | main | behavior reference only |
| current Automatone source | local worktree | dirty, unaccepted | src/main/java/baritone/utils/InputOverrideHandler.java; src/main/java/baritone/utils/PathingControlManager.java | existing input/control symbols to inspect |
| current Automatone source | local worktree | dirty, unaccepted | src/main/java/baritone/pathing/path/PathExecutor.java | native path executor to observe, not replace |
| future consumer source | repository | UNKNOWN | WorkerEntity control/tick package | exact package path is not established |
| future test fixture | repository | UNKNOWN | movement/navigation GameTest path | exact fixture path is not established |

## Allowed scope

- Verify Automatone forward, sideways, jump, and look input moves the custom
  entity.
- Prevent vanilla goals, navigation, MoveControl, LookControl, and JumpControl
  from overwriting active Automatone decisions.
- Prefer solving conflicts in WorkerEntity, not global mixins.
- Define inactive behavior as simply standing still.
- Confirm cancel clears stale movement.

## Explicitly forbidden / out of scope

- Fake clients, fake players, packet-driven movement, or client authority.
- Global mob-control mixins when worker-local control can express the invariant.
- Custom pathfinding or movement scripts.
- Combat, wandering, following, storage, GUI, networking, quantity, or
  persistence behavior.
- GUI work before Milestone 3 passes.

The full global architecture contract, quantity semantics, and excluded-feature
list are preserved in INDEX.md.

## Preflight questions

1. Which current input/control symbols deliver native movement decisions?
2. Which entity tick, navigation, and vanilla control callers can overwrite them?
3. Does the worker already have a local suppression hook?
4. Which architecture boundary would a global mixin cross?
5. What GameTest observes position, rotation, jump, idle, and cancellation over
   bounded ticks?
6. Are hitbox, raycast, and step-height assumptions established or UNKNOWN?

## Implementation constraints

- Native Automatone remains the source of movement decisions.
- Apply movement on the server/main thread through the worker.
- Resolve control conflicts locally where possible.
- Inactive means standing still.
- Cancellation must clear stale movement input and not modify world state.

## Acceptance criteria and evidence mapping

| ID | Acceptance criterion | Required sensor / evidence | Blocking? |
| --- | --- | --- | --- |
| AC-1 | Native movement inputs move the worker deterministically. | neoforge_gametest | yes |
| AC-2 | Vanilla controls cannot overwrite active decisions. | neoforge_gametest | yes |
| AC-3 | Conflict resolution is worker-local and no broad mixin is added. | archunit; source/mixin review | yes |
| AC-4 | Idle and cancellation leave no stale movement. | neoforge_gametest | yes |

**Done when (plan text):** the worker moves deterministically without packets,
fake clients, jitter, or vanilla navigation taking control.

## Required sensor profile

- default
- architecture_sensitive
- runtime_minecraft

Runtime movement evidence is required; compile-only proof is insufficient.

## Expected touched areas

- src/main/java/baritone/utils/InputOverrideHandler.java only if a verified
  native input contract requires integration.
- src/main/java/baritone/utils/PathingControlManager.java only if the native
  control handoff requires it.
- UNKNOWN: exact WorkerEntity control/tick implementation path.
- UNKNOWN: exact movement GameTest fixture path.

## Failure / blocker conditions

- Fake-client/player or packet authority is required.
- Global mixin becomes the only way to suppress vanilla control.
- Custom pathfinding or movement script is proposed.
- Idle/cancel movement cannot be observed deterministically.
- Required runtime sensor is unavailable.

## Milestone completion gate

M2 cannot be complete until all of these plan gates pass:

- One worker equals one runtime.
- Native Automatone moves the worker to a simple goal.
- Simple jump or step traversal works.
- No fake client or player input layer exists.
- Cancellation or removal leaves the worker idle and the runtime disposed.
- Movement GameTests pass consistently.

M2.3 supplies the direct-control portion; M2.4 supplies the scenario matrix.

## Completion evidence

The final verifier report must record:

- accepted candidate commit SHA;
- changed files;
- acceptance criterion to evidence mapping;
- exact commands and checks executed;
- sensor statuses;
- fresh-context verification result;
- remaining warnings or UNKNOWNs;
- any approved waivers.
