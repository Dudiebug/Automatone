---
id: "M2.4"
title: "Navigation GameTests"
state: "COMPLETE"
milestone: "M2"
depends_on:
  explicit: []
  inferred:
    - task: "M2.3"
      reason: "INFERRED scheduling edge from the plan's Recommended implementation order; the plan states no explicit task-level dependency."
risk: "high"
sensor_profiles:
  - "default"
  - "architecture_sensitive"
  - "runtime_minecraft"
implementation_plan: "docs/M2_WORKER_IMPLEMENTATION_PLAN.md"
plan_reference:
  document: "docs/NEOFORGE_1.21.1_SERVER_WORKER_MILESTONES.md"
  sections:
    - "337-346: M2.4 — Navigation GameTests"
    - "348-369: Milestone 2 runnable acceptance test, risks, and completion gate"
objective: "Provide dedicated-server GameTests proving native Automatone navigation, simple obstacle traversal, cancellation, and worker/runtime cleanup."
allowed_scope:
  - "flat 5-8 block goal;"
  - "one simple step/jump obstacle;"
  - "cancellation while moving;"
  - "removal/disposal after movement."
  - "Use native Automatone goal/pathing behavior, not a custom movement script."
forbidden_scope:
  - "Do not add custom A*, pathfinding, navigation, movement scripts, or target queues."
  - "Do not use a fake client/player or packet-driven movement."
  - "Do not add product mining, quantity, GUI, networking, or persistence."
  - "Do not begin GUI work before Milestone 3 passes."
  - "Do not mark unavailable runtime fixtures as PASS."
acceptance_criteria:
  - id: "AC-1"
    statement: "Native Automatone reaches a flat goal 5-8 blocks away."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Dedicated-server GameTest observes worker arrival within a bounded allowance."
  - id: "AC-2"
    statement: "Native Automatone traverses one simple step or jump obstacle."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Obstacle GameTest observes successful traversal and goal completion."
  - id: "AC-3"
    statement: "Cancellation while moving stops stale movement and leaves the worker idle."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Post-cancel observation window records no continued movement."
  - id: "AC-4"
    statement: "Worker removal/disposal after movement leaves no live runtime."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Removal GameTest observes runtime disposal/unregistration."
  - id: "AC-5"
    statement: "The tests exercise native Automatone goal/pathing rather than a custom movement script."
    sensor: "archunit"
    blocking: true
    evidence: "Source/architecture review finds no consumer pathfinder or scripted route."
blocker_conditions:
  - "The GameTest fixture or dedicated-server command is unavailable."
  - "A scenario passes only with fake-client input, packet injection, or a custom movement script."
  - "Simple step/jump behavior depends on unverified hitbox or step-height assumptions."
  - "Cancellation/removal leaves stale movement or a live runtime."
  - "A test is weakened to avoid a failing native behavior."
---

# M2.4 — Navigation GameTests

## Approved implementation detail

The user-approved [M2 implementation plan](../../docs/M2_WORKER_IMPLEMENTATION_PLAN.md), section M2.4,
resolves the module, files, defaults, acceptance measurements, and stopping points.
M2.3 is COMPLETE at `cf3783b5`; QUALITY-CLEANUP is ACCEPTED. Native navigation focused checks and three consecutive fresh server runs passed; M2.4 is COMPLETE and the M2 gate remains PENDING.

## Metadata

- State: COMPLETE
- Milestone: M2
- Depends on: INFERRED M2.3 scheduling edge; no explicit task-level dependency
  is stated by the original milestone document. The approved M2 implementation
  plan now requires M2.3 acceptance before execution.
- Risk: high
- Sensor profiles: default, architecture_sensitive, runtime_minecraft
- Approved plan reference: plan lines 337-369.

## Objective

Provide dedicated-server GameTests proving native Automatone navigation, simple
obstacle traversal, cancellation, and worker/runtime cleanup.

## Why this task exists

The worker movement contract is only useful if it works through native goals and
pathing in the real server runtime. A small scenario matrix catches input
overwrites, obstacle assumptions, stale movement, and lifecycle leaks before
mining is attempted.

These tests are evidence for the M2 gate, not a new navigation implementation.

## Source and documentation references

| Type | Repository / source | Ref / version | Path / symbol | Why relevant |
| --- | --- | --- | --- | --- |
| plan | local approved plan | current | docs/NEOFORGE_1.21.1_SERVER_WORKER_MILESTONES.md, lines 337-346 | exact GameTest checklist |
| plan | local approved plan | current | Milestone 2 acceptance/gate, lines 348-369 | runtime command and gate |
| source baseline | Dudiebug/Automatone | 545c552d6f3c333e32a256dd82227004954dd5c3 | fork 1.20 branch, Minecraft 1.20.1 | immutable source baseline |
| planning baseline | Dudiebug/Automatone | d555e41d0beb96194de5ff7c0a8dac7b701d0581 | planning baseline | immutable planning reference |
| behavioral reference | minefortress-mod/automatone | 58d090edaa3b8fb6cfdd3b1fecacf41f60ab8d4c | main | behavior reference only |
| current Automatone source | candidate commit | e81dbad7, unaccepted | src/main/java/baritone/pathing/path/PathExecutor.java; src/main/java/baritone/pathing/movement | native pathing to exercise, not replace |
| planned test fixture | approved M2 plan | M2.4 | worker/src/gameTest/java/automatone/worker/gametest/WorkerNavigationGameTest.java; worker GameTest structures/build configuration | native navigation scenario matrix |

## Allowed scope

- Flat 5-8 block goal.
- One simple step or jump obstacle.
- Cancellation while moving.
- Removal/disposal after movement.
- Native Automatone goal/pathing behavior only.

## Explicitly forbidden / out of scope

- Custom A*, pathfinding, navigation, movement scripts, or target queues.
- Fake-client/player or packet-driven movement.
- Product mining, quantity, GUI, networking, or persistence.
- GUI work before Milestone 3 passes.
- Passing an unavailable runtime fixture or weakened test.

The full global architecture contract, quantity semantics, and excluded-feature
list are preserved in INDEX.md.

## Preflight questions

1. Which native goal/pathing entry points can the fixture invoke?
2. Which worker/runtime lifecycle symbols does each scenario exercise?
3. Does a navigation GameTest fixture already exist?
4. Which test setup could accidentally provide a custom route or client input?
5. What bounded arrival, obstacle, cancel, and removal assertions observe
   success?
6. Which hitbox, step-height, or rotation assumptions are UNKNOWN?

## Implementation constraints

- Use the real dedicated-server runtime and native goal/pathing behavior.
- Keep scenarios small, deterministic, and bounded.
- Do not add test-only movement code that changes product ownership.
- Mark unavailable runtime checks UNVERIFIED.
- Preserve cancellation and disposal evidence for later task gates.

## Acceptance criteria and evidence mapping

| ID | Acceptance criterion | Required sensor / evidence | Blocking? |
| --- | --- | --- | --- |
| AC-1 | Flat 5-8 block native goal succeeds. | neoforge_gametest | yes |
| AC-2 | Simple step/jump obstacle succeeds. | neoforge_gametest | yes |
| AC-3 | Cancel while moving stops stale movement. | neoforge_gametest | yes |
| AC-4 | Removal/disposal leaves no live runtime. | neoforge_gametest | yes |
| AC-5 | Tests use native pathing, not consumer movement code. | archunit; source review | yes |

The plan provides this checklist rather than a separate M2.4 Done when
sentence. The checklist plus the Milestone 2 gate is the acceptance boundary.

## Applicable milestone profiles

- default
- architecture_sensitive
- runtime_minecraft

The runtime_minecraft profile is blocking. An unavailable GameTest command or
fixture is UNVERIFIED.

During this task, select focused acceptance checks under EXECUTION_STRATEGY.md;
justify earlier broad checks by concrete risk and defer the remaining profile
checks as PENDING until the single M2 gate.

## Expected touched areas

- worker/src/gameTest/java/automatone/worker/gametest/WorkerNavigationGameTest.java.
- Worker GameTest corridor structure and build configuration.
- Reuse the worker spawn helper. Production defects return to their owning task.

## Failure / blocker conditions

- Dedicated-server runtime/fixture is unavailable.
- Scenario needs fake-client input or custom pathing.
- Obstacle behavior is nondeterministic or based on invented physics.
- Cancellation/removal leaks movement or runtime state.
- Required runtime sensor is UNVERIFIED.

## Milestone completion gate

M2 cannot be complete until all of these plan gates pass:

- One worker equals one runtime.
- Native Automatone moves the worker to a simple goal.
- Simple jump or step traversal works.
- No fake client or player input layer exists.
- Cancellation or removal leaves the worker idle and the runtime disposed.
- Movement GameTests pass consistently.

M2.4 supplies the scenario-level evidence for this gate.

## Completion evidence

Use one concise task evidence record; the single fresh M2 gate supplies milestone-wide evidence:

- candidate identity;
- changed files;
- acceptance criterion to evidence mapping;
- exact commands and checks executed;
- sensor statuses;
- PENDING milestone checks (fresh-context verification is required once at M2 completion);
- remaining warnings or UNKNOWNs;
- any approved waivers.
