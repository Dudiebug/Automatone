---
id: "M2.2"
title: "Attach one explicit Automatone runtime"
state: "BLOCKED"
milestone: "M2"
depends_on:
  explicit: []
  inferred:
    - task: "M2.1"
      reason: "INFERRED scheduling edge from the plan's Recommended implementation order; the plan states no explicit task-level dependency."
risk: "high"
sensor_profiles:
  - "default"
  - "architecture_sensitive"
  - "runtime_minecraft"
implementation_plan: "docs/M2_WORKER_IMPLEMENTATION_PLAN.md"
plan_reference:
  document: "docs/NEOFORGE_1.21.1_SERVER_WORKER_MILESTONES.md"
  sections:
    - "284-304: Milestone 2 goal, required modules, and integration replacements"
    - "317-325: M2.2 — Attach one explicit Automatone runtime"
objective: "Attach exactly one explicitly owned Automatone runtime to each loaded worker, tick it once per server tick, and dispose or recreate it with the worker lifecycle."
allowed_scope:
  - "Construct exactly one runtime per loaded worker."
  - "Supply the worker entity context and controller explicitly."
  - "Tick the runtime exactly once per server tick."
  - "Cancel/unregister on unload/removal."
  - "Recreate one fresh transient runtime after reload."
forbidden_scope:
  - "Do not use automatic CCA/component lookup or duplicate runtime ownership."
  - "Do not persist active runtime/path/process internals."
  - "Do not add a worker fleet, multiple runtimes per worker, job queue, or offline operation."
  - "Do not implement movement, mining, GUI, networking, or quantity logic in this lifecycle task."
  - "Do not begin GUI work before Milestone 3 passes."
acceptance_criteria:
  - id: "AC-1"
    statement: "Exactly one live Automatone runtime is mapped to each loaded worker, with no duplicate creation."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Server test loads a worker, observes one runtime, repeats lookup/attachment, and observes no duplicate."
  - id: "AC-2"
    statement: "The runtime receives the worker entity context and controller through explicit ownership."
    sensor: "archunit"
    blocking: true
    evidence: "Dependency/source report shows explicit constructor/factory wiring rather than component lookup."
  - id: "AC-3"
    statement: "Each runtime advances exactly once per server tick."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Bounded server tick test correlates server ticks with runtime tick count."
  - id: "AC-4"
    statement: "Unload or removal cancels/unregisters the runtime and leaves no live owner."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Removal/unload test observes cancellation, unregistration, and disposal."
  - id: "AC-5"
    statement: "Reload creates one fresh transient runtime without restoring active path or process internals."
    sensor: "neoforge_gametest"
    blocking: true
    evidence: "Lifecycle test distinguishes the new runtime identity from the disposed runtime and checks transient state."
blocker_conditions:
  - "One worker can acquire multiple live runtimes or receive duplicate ticks."
  - "Lifecycle requires CCA/component lookup, persistent process state, or chunk loading."
  - "Unload/removal cannot dispose the runtime deterministically."
  - "Fresh reload semantics would silently auto-resume work."
  - "The approved lifecycle hooks or reload fixture cannot demonstrate singular transient ownership."
---

# M2.2 — Attach one explicit Automatone runtime

## Approved implementation detail

The user-approved [M2 implementation plan](../../docs/M2_WORKER_IMPLEMENTATION_PLAN.md), section M2.2,
resolves the module, files, defaults, acceptance measurements, and stopping points.
M2.1 is COMPLETE at `175290553e2b29891c4c7f14078dc9f4b42204b6`; QUALITY-CLEANUP remains ACCEPTED.

## Metadata

- State: BLOCKED
- Milestone: M2
- Depends on: INFERRED M2.1 scheduling edge; no explicit task-level dependency
  is stated by the original milestone document. The approved M2 implementation
  plan now requires M2.1 acceptance before execution.
- Risk: high
- Sensor profiles: default, architecture_sensitive, runtime_minecraft
- Approved plan reference: plan lines 284-325.

## Objective

Attach exactly one explicitly owned Automatone runtime to each loaded worker,
tick it once per server tick, and dispose or recreate it with the worker
lifecycle.

## Why this task exists

The worker must not rely on automatic component attachment. Explicit ownership
makes duplicate runtime creation, duplicate ticks, unload cleanup, and reload
reconstruction observable and bounded.

The runtime is transient. Persistence of product state and restart semantics
are Milestone 6 concerns, not a reason to serialize path or process internals.

## Source and documentation references

| Type | Repository / source | Ref / version | Path / symbol | Why relevant |
| --- | --- | --- | --- | --- |
| plan | local approved plan | current | docs/NEOFORGE_1.21.1_SERVER_WORKER_MILESTONES.md, lines 284-304 | explicit runtime architecture |
| plan | local approved plan | current | M2.2 task, lines 317-325 | exact lifecycle checklist and done-when |
| source baseline | Dudiebug/Automatone | 545c552d6f3c333e32a256dd82227004954dd5c3 | fork 1.20 branch, Minecraft 1.20.1 | immutable source baseline |
| planning baseline | Dudiebug/Automatone | d555e41d0beb96194de5ff7c0a8dac7b701d0581 | planning baseline | immutable planning reference |
| behavioral reference | minefortress-mod/automatone | 58d090edaa3b8fb6cfdd3b1fecacf41f60ab8d4c | main | behavior reference only |
| current Automatone source | candidate commit | e81dbad7, unaccepted | src/main/java/baritone/BaritoneProvider.java#BaritoneProvider | existing explicit runtime owner to inspect |
| current Automatone source | candidate commit | e81dbad7, unaccepted | src/main/java/baritone/Baritone.java#Baritone | existing runtime to attach, not accepted evidence |
| planned consumer source | approved M2 plan | M2.2 | worker/src/main/java/automatone/worker/WorkerEntity.java; WorkerContext.java; WorkerEntityController.java | explicit host attachment |
| planned test fixture | approved M2 plan | M2.2 | worker/src/gameTest/java/automatone/worker/gametest/WorkerRuntimeGameTest.java | unload/reload and tick evidence |

## Allowed scope

- Construct exactly one runtime per loaded worker.
- Supply the worker entity context and controller explicitly.
- Tick the runtime exactly once per server tick.
- Cancel or unregister on unload or removal.
- Recreate one fresh transient runtime after reload.

## Explicitly forbidden / out of scope

- Automatic CCA/component lookup or duplicate runtime ownership.
- Persisting active runtime, path, or process internals.
- Worker fleets, multiple runtimes per worker, job queues, or offline operation.
- Movement, mining, GUI, networking, or quantity logic beyond lifecycle wiring.
- GUI work before Milestone 3 passes.

The full global architecture contract, quantity semantics, and excluded-feature
list are preserved in INDEX.md.

## Preflight questions

1. Which current provider/runtime symbols should own the worker attachment?
2. Which worker lifecycle callbacks and server tick event can invoke them?
3. Does existing explicit provider logic already prevent duplicate contexts?
4. Which unload/reload edge can cause stale runtime retention or duplicate ticks?
5. What server test directly observes one runtime, one tick, removal, and fresh
   recreation?
6. Confirm the approved entity add/remove hooks, stable context identity, and
   save/remove/load fixture can exercise all five lifecycle criteria.

## Implementation constraints

- Runtime ownership is explicit and singular.
- Tick the runtime once per server tick; avoid recursive or duplicate lookups.
- Runtime state is transient and must not become a persistence engine.
- Removal/disposal is deterministic and idempotent.
- No chunk tickets, offline work, auto-resume, or product queue.

## Acceptance criteria and evidence mapping

| ID | Acceptance criterion | Required sensor / evidence | Blocking? |
| --- | --- | --- | --- |
| AC-1 | One worker maps to exactly one live runtime. | neoforge_gametest; lifecycle identity assertion | yes |
| AC-2 | Context/controller are supplied explicitly. | archunit; dependency/source review | yes |
| AC-3 | Runtime ticks once per server tick. | neoforge_gametest | yes |
| AC-4 | Unload/removal disposes and unregisters. | neoforge_gametest | yes |
| AC-5 | Reload creates one fresh transient runtime. | neoforge_gametest; entity save/remove/load | yes |

**Done when (plan text):** one loaded worker always maps to exactly one live
runtime.

## Applicable milestone profiles

- default
- architecture_sensitive
- runtime_minecraft

Required runtime lifecycle evidence is blocking and cannot be replaced with
compile-only evidence.

During this task, select focused acceptance checks under EXECUTION_STRATEGY.md;
justify earlier broad checks by concrete risk and defer the remaining profile
checks as PENDING until the single M2 gate.

## Expected touched areas

- worker/src/main/java/automatone/worker/WorkerEntity.java.
- worker/src/main/java/automatone/worker/WorkerContext.java and WorkerEntityController.java.
- worker/src/gameTest/java/automatone/worker/gametest/WorkerRuntimeGameTest.java and controller tests.
- Existing provider and server post-tick source are references, not redesign targets.

## Failure / blocker conditions

- Duplicate runtime or duplicate tick is observed.
- Unload/removal leaks a live runtime.
- Reload silently resumes work or restores transient internals.
- CCA/component ownership or chunk-loading/offline behavior is introduced.
- Required runtime sensor is unavailable.

## Milestone completion gate

M2 cannot be complete until all of these plan gates pass:

- One worker equals one runtime.
- Native Automatone moves the worker to a simple goal.
- Simple jump or step traversal works.
- No fake client or player input layer exists.
- Cancellation or removal leaves the worker idle and the runtime disposed.
- Movement GameTests pass consistently.

M2.2 supplies only the lifecycle portion of this gate.

## Completion evidence

Use one concise task evidence record; the single fresh M2 gate supplies milestone-wide evidence:

- candidate identity;
- changed files;
- acceptance criterion to evidence mapping;
- exact commands and checks executed;
- sensor statuses;
- PENDING milestone checks (fresh-context verification is required once at M2 completion);
- remaining warnings or UNKNOWNs;
- any approved waivers.

Focused behavior criteria pass; two new live-identity SpotBugs findings remain blocking. See [M2.2 evidence and decision](../decisions/M2.2-EVIDENCE.md). M2.3 has not started.
