# M5.5 — Server menus and validated networking

Supersedes historical M5.5 task text under human-approved docs/M5_GUI_CONTROLLER.md.
Dependency: M5.4. Follow AGENTS.md and EXECUTION_STRATEGY.md.

## Objective

Implement native inventory menus, scoped roster handles, snapshots/revisions and bounded intent payloads for all approved actions. Enforce current controller/menu/owner/revision before mutations.

## Scope

Worker menu/network/server action code and focused authorization/inventory tests. Screens later.

## Acceptance and focused checks

Malformed/stale/non-owner/duplicate requests leave authoritative state unchanged; cross-dimension access works; active and retired slot transfers cannot duplicate; busy replacement confirmation verified.

## Astra's focused testing foundation

- Use real dedicated-server GameTests and actual WorkerMenu/WorkerRoster/native Slot
  operations. The only allowed substitute is the network transport sink for a real
  ServerPlayer/ServerGamePacketListenerImpl; it may capture outbound packets without
  a GPU client. Do not mock authorization, inventories, workers, native processes,
  settings, registry validation, menu clicks or the action executor.
- Open with the real controller and WorkerMenu.open. Snapshot data exposes the
  server revision/response; each intent requires the current menu id, random session
  token and next strictly increasing sequence. New selection opens a new native
  menu id. Use actual stale ServerboundContainerClickPacket handling to exercise
  the registered mixin, not only WorkerMenu.acceptsClick.
- Prove non-owner, old menu/token, absent controller, stale worker/profile revision,
  duplicate sequence, invalid registry/quantity/shape and bounded codec rejection
  leave worker/job/settings/inventory state unchanged. Session sequencing itself is
  transport bookkeeping, not product mutation. Cross-dimension owned access succeeds.
- Prove item conservation across active pickup/quick-move and archived withdrawals;
  forbid archive insertion through ordinary click, hotbar swap, merging quick-move
  and creative cloning. Stale state-id clicks must not mutate before resynchronizing.
  Pending RTP blocks slot and selected-worker job mutations; cancellation restores
  access. Archives save/reload with the remaining inventory.
- Batch preview stores recipients, revisions, run identities, job and start choice
  on the server. Invalid token, changed recipient or stale revision rejects the whole
  commit before the first mutation. A valid commit replaces busy jobs explicitly,
  preserves personal/worker overrides, reports each execution result and cannot run
  twice. Test both apply-only and apply-and-start outcomes.
- Fixtures own unique owners, workers, real player menus and pending RTP requests.
  Close menus/return carried stacks, cancel requests and discard only fixture-owned
  workers in success and failure paths; restore any creative/player/world setting.
- Luna owns only WorkerMenuGameTest.java; Astra owns production and reviews the tests.
  Run only namespace automatone_worker_m5_menu_gametest after production compiles.
  Escalate API ambiguity, spec contradiction, production failures or stalled work
  directly to Astra; do not change production, weaken expectations or redelegate.

Record actual checks in .agents/decisions/M5-EVIDENCE.md. Task completion is not
milestone acceptance; deferred broad checks remain PENDING. No duplicate native
scanner/pathfinder/mining engine, no client authority, no starter equipment.
