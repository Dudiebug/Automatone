# M5.1 — Runtime settings and multi-target job foundation

Supersedes historical M5.1 task text under human-approved docs/M5_GUI_CONTROLLER.md.
Dependency: M4.5. Follow AGENTS.md and EXECUTION_STRATEGY.md.

## Objective

Isolate native runtime settings and async calculation snapshots; expand jobs to immutable target lists, run identity and PAUSED state. Preserve single-target API/save compatibility. Pause synchronously cancels native work, resume retains progress, replan retains progress; stop ends run and start resets it.

## Scope

src/api, src/main native settings call paths; worker MiningSession/WorkerEntity/WorkerContext; focused unit/GameTests. No GUI, roster or RTP implementation in this task.

## Acceptance and focused checks

Unit checks: copy isolation including mutable setting values; mixed-target progress/unlimited/finite bounds; pause/resume/stop transitions. Server checks: independent worker calculations, mixed actual breaks stop at N, pause prevents stale native breaks and resume completes remainder. Existing affected M4 checks remain valid.

Record actual checks in .agents/decisions/M5-EVIDENCE.md. Task completion is not
milestone acceptance; deferred broad checks remain PENDING. No duplicate native
scanner/pathfinder/mining engine, no client authority, no starter equipment.

