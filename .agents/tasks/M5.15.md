# M5.15 — Overview relocation and visible scrolling

Status: IMPLEMENTING. Baseline `8d8184fb` (0.12.0 prerelease).
Authority: the human-approved overview relocation/scrolling plan in this task.

Only add overview Relocate selected and a visible draggable worker scrollbar
with track clicks, wheel input and Showing first–last of total. Preserve selection
and clamp on resize/live updates; no hidden footer controls at compact sizes.

Extend existing fleet preview/confirmation with RELOCATE + Dimension and the
existing WorkerBatch queue with relocation children. Validate all ownership and
revision references before confirmation mutation; pause eligible workers when
queued. Queued/preparing workers cannot be resubmitted or mutated through other
controller paths. Dispatch through WorkerRelocation's shared four-preparer limit,
without new supplies or roster reservations. Preserve identity/items/progress;
individual failure does not cancel siblings. Close continues; logout/shutdown
cancel unfinished work and leave existing workers and contents, with jobs paused.
Expose per-worker progress and cancellation in the existing Progress view.

Reuse individual relocation and bounded server-owned confirmation validation.
Ship matching 0.12.1 JARs/protocol 5 with checksums and a short human checklist.
No new dependencies, world migration, native engine changes or other UI redesign.

Subsequent human report adds a Collection repair: item clicks fail to highlight
and transfer does not move items while workers update. Separate query/scope
revision from exact source-content revision so ordinary selection/transfer use
fresh live amounts without racing mining pickups. Keep stable component variant
identities, ownership and full-inventory behavior. Retirement confirmation still
rejects changed contents; old queries still reject stale scope requests. Add a
focused regression for an inventory update between snapshot/click/transfer.

Astra owns production, integration and non-game checks. Bounded Luna owns only
added relocation regressions in WorkerBatchGameTest.java, reusing real menu,
roster, inventories and existing deterministic destination fixtures. Cover >4
queue/pause/preservation, duplicate/stale/foreign rejection, close and cancellation.
No mock inventories, authorization or movement. Helpers do not redelegate or
modify production. Do not launch any Minecraft client/server/GameTest/restart
probe; regression source may be compiled, runtime stays PENDING — HUMAN TESTING.
Use focused compilation/unit/static checks proportional to queue/server risk;
append results to the existing global management evidence record. This follow-up
does not accept M5 or require a repeat of its full milestone profile.
