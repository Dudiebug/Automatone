# M5.14 — Non-game integration gate and handoff

Status: implementation and delivery COMPLETE; in-game acceptance PENDING — HUMAN TESTING.
Prerequisite M5.13 `b34a3ea0` implementation COMPLETE.
Authority: `docs/M5_GLOBAL_MANAGEMENT.md` plus the subsequent explicit human
instruction assigning ALL in-game testing to the human (EXECUTION_STRATEGY.md).

Ship matching version 0.12.0 native/worker JARs with protocol 4, checksums and a
numbered in-game checklist/user guide. Preserve prior 0.11.1 release artifacts.
Update the structural graph at this completed feature boundary.

One fresh independent verifier from a clean candidate supplies default and
architecture_sensitive profiles once (sensorCheck). No Minecraft clients,
GameTest servers, runtime profiles or restart probes. Compile GameTest source
as code if necessary, but do not execute it. Repair affected non-game failures
and rerun only invalidated checks. Do not relax sensors or call pending in-game
checks PASS. Controller owns final integration/package inspection. All in-game
acceptance stays PENDING — HUMAN TESTING and does not block build delivery.

Independent non-game profile PASS on clean candidate `e987600a`; controller
package inspection PASS. Both JARs, SHA256 manifest and the 12-step human checklist
are in `dist/automatone-0.12.0-global-management.zip`. See the single evidence
record `.agents/decisions/M5-GLOBAL-MANAGEMENT-EVIDENCE.md` for checks and limits.
