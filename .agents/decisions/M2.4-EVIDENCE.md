# M2.4 native navigation evidence

Status: COMPLETE. Baseline: M2.3
`cf3783b5c9ffaf904b4dfc16c16914e9b0c60651`.

Acceptance: native GoalBlock six blocks away, native one-block-rise corridor
traversal with unchanged terrain, grounded cancellation after movement with
at most 0.01 block drift for 20 ticks, and removal after movement with disposal,
provider removal and no subsequent runtime events. Only fixture setup precedes
native start; afterward tests observe or invoke native cancel/removal. Three
consecutive fresh worker server processes are required.

Graphify scoped preflight located CustomGoalProcess, PathingBehavior and
PathExecutor as native owners. Source confirms native goal completion is
reported when both host feet and path start satisfy the goal; the fixture must
observe actual path execution and process completion, not only position.
Terra authored WorkerNavigationGameTest.java and worker_navigation.snbt;
Astra reviewed scope, assertions and evidence. No production code changed.

Focused checks (2026-09-05):

- `./gradlew.bat :worker:runGameTestServer :worker:checkstyleGameTest
  --no-daemon --console=plain` exited 0; all 14 worker GameTests passed and
  GameTest Checkstyle passed. This compiles the new tests against the real
  worker and library. Log: `.agents/evidence/M2.4/logs/navigation-tests-runtime-fixture-repair.log`.
- Two subsequent fresh processes ran `./gradlew.bat :worker:runGameTestServer
  --no-daemon --console=plain`, each exiting 0 with all 14 tests passing.
  Logs in the same directory: `navigation-tests-runtime-run2.log` and
  `navigation-tests-runtime-run3.log`. These are three consecutive successful
  server runs of the final fixture, not retries hiding failures.
- The initial run failed all four new cases at fixture readiness before native
  start (worker not grounded); the ten existing cases passed. Retained log:
  `navigation-tests-runtime-first.log`. Its exact static-template/spawn cause
  remains unproven. The final fixture uses a bounded air structure, interior
  initial positioning, then builds its stone corridor before native start.
  No navigation tolerance or product behavior was changed to obtain PASS.
- AC-1/2 PASS: observed native goal activity, native path execution and process
  completion at a six-block flat target and raised goal. The latter requires
  actual ascent and a one-cell corridor preventing bypass. Both compare all
  225 fixture cells with expected terrain after arrival.
- AC-3 PASS: native cancellation occurs only after measured grounded path
  progress before arrival; all native inputs clear by the next entity tick,
  the custom process/path are inactive, and horizontal drift is at most
  0.01 block over the next 20-tick grounded window.
- AC-4 PASS: removal follows real path movement and live runtime events;
  runtime reference clears, runtime is disposed/unregistered, and subsequent
  PRE/POST event counts remain unchanged over real server ticks.
- AC-5 PASS (controller source review plus reused M2.3 architecture evidence):
  all four starts invoke native `setGoalAndPath(GoalBlock)`. After start the
  tests observe or invoke native cancel/removal; no supplied path, movement
  sequence, teleport, consumer engine or client input is used. Production
  source is unchanged from M2.3, whose four architecture tests still apply.
- Fixture settings (allowBreak/allowPlace) and terrain restore on success and
  bounded failure; only the fixture's worker/runtime is removed. Every case
  has a distinct sequential batch in the worker-only server. Scoped whitespace
  check passed. No baseline analyzer policy or assertion threshold changed.

Controller confirms M2.4 COMPLETE with AC-1 through AC-5 established. Native
navigation required no new production implementation. Graphify incremental
refresh is performed before freezing the final candidate; known Groovy parse
limitations remain advisory.

Deferred: one fresh independent clean M2 candidate gate covering the union of
default, architecture_sensitive, runtime_minecraft and dependency_change,
both artifacts/native ownership, and the two already approved exact M2.2
WorkerContext exceptions. No M3 work is authorized in this run.
