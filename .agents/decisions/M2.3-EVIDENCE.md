# M2.3 direct server movement evidence

Status: COMPLETE. Baseline: M2.2 `b53facf1693249f32d5648b7de73b5b43980abe5`.

Changes and acceptance mapping:

- AC-1: Worker travel reads its movement-speed attribute without calling
  `Mob.setSpeed`, which would overwrite native forward input. Vanilla travel,
  collision, gravity and jumping remain in `super.aiStep()`.
- AC-2/3: Only this worker's MoveControl, LookControl and JumpControl ticks are
  inert. Vanilla navigation stops before its AI phase. Existing goal selectors
  remain empty. No global mixin, player, client, packet control or native engine
  implementation is introduced.
- AC-4: Grounded workers with no directional input clear horizontal velocity
  before travel while retaining vertical physics. Cancellation remains native
  `getPathingBehavior().cancelEverything()`.

Source preflight: scoped Graphify queries located the existing native input,
look and cancellation owners. The pinned NeoForge 21.1.249 source artifact
confirms `Mob.serverAiStep()` is final, runs navigation and controls inside
`LivingEntity.aiStep()`, and `Mob.setSpeed()` changes forward input. Existing
`InputOverrideHandler` applies host fields and clears them on native cancellation;
`LookBehavior.applyServerTick()` applies native server rotations.

Focused checks and controller review (2026-09-05):

- `./gradlew.bat :worker:compileJava :worker:checkstyleMain :worker:sensorTest
  --no-daemon --console=plain`: fresh WorkerEntity bytecode and Checkstyle report
  were produced; the architecture XML records 4 tests, zero failures/errors/skips.
  The console tail/process exit was not retained, so this entry claims the
  inspected reports, not a captured successful exit. Reports are retained under
  `.agents/evidence/M2.3/worker-{architecture,checkstyle-main}.xml`.
- Terra authored `WorkerMovementGameTest.java` and `worker_movement.snbt`.
  `./gradlew.bat :worker:runGameTestServer :worker:checkstyleGameTest
  --no-daemon --console=plain` exited 0: all 10 worker GameTests passed (three new
  movement cases plus seven existing host/runtime cases), and GameTest style
  passed. Final raw log:
  `.agents/evidence/M2.3/logs/movement-tests-runtime-fixture-repair.log`.
  Compilation retains the 12 existing runtime-test identity warnings.
- AC-1: native forward/sideways displacement, jump height and server yaw/pitch
  were observed over bounded real ticks. AC-2: queued opposing vanilla move,
  look, jump and navigation requests did not zero native input, reclaim yaw,
  cause a jump, or interrupt the two movement observation windows.
- AC-3: four worker architecture checks pass; controller source review confirms
  the change is local to WorkerEntity, goal selectors remain empty, vanilla
  physics is retained, and no native responsibility or global mixin was added.
- AC-4: initial idle and native `cancelEverything()` both passed the grounded
  observations. Cancellation cleared directional/jump input and horizontal
  velocity by the next entity tick and remained within 0.01 block over 20 ticks.
  Tests did not manually clear keys to obtain cancellation success.
- Initial test failures are retained in the logs: an axis assertion contradicted
  the requested 90-degree yaw; after correction, the one-block smoke fixture's
  GameTest enclosure blocked further motion at exactly 0.2 block displacement.
  Pinned GameTestInfo calls `StructureUtils.encaseStructure`. Terra replaced
  that unsuitable movement fixture with a 13x4x13 structure and interior spawn.
  No product requirement or assertion tolerance was weakened. No native
  cancellation defect was reproduced, so no native code was changed.
- Graphify incremental updates passed with the same seven known partial Groovy
  parse limitations. Scoped product whitespace check passed.

Controller confirms focused AC-1 through AC-4 and scope/ownership invariants.
No M2.3 production repair beyond the handed-off WorkerEntity changes was needed.
The movement changes were already present before these tests, so a pre-change
behavioral RED is not claimed. The measured fixture failures are not product RED.

Deferred: complete M2 profile and one fresh independent gate after M2.4, including
independent confirmation of the two human-approved M2.2 ownership exceptions.
Native goal navigation and repeatability are M2.4; no M3 mining work is included.
