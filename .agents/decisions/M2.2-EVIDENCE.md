# M2.2 evidence and ownership decision

Status: COMPLETE. Focused product criteria and the human-approved exact exception gate pass; milestone acceptance remains PENDING. Baseline: M2.1 `175290553e2b29891c4c7f14078dc9f4b42204b6`. Parent implemented lifecycle/context/controller code; Luna authored and executed the tests. Test changes are in `worker/src/gameTest/java/automatone/worker/gametest/WorkerRuntimeGameTest.java` and `WorkerGameTestSupport.java`. No M2.3 implementation has started. Raw artifact paths below are relative to `.agents/evidence/M2.2/`.

Focused acceptance results:

- AC-1 PASS: one loaded worker retained one provider runtime, stable context/controller identity, repeated attachment, and baseline-relative provider membership.
- AC-2 PASS (focused evidence): runtime context exposes the worker container and stable controller; the existing architecture sensor XML records 4 tests, 0 failures, 0 errors, 0 skips.
- AC-3 PASS: the runtime tick test observed exactly 20 PRE/IN and 20 POST/IN events over 20 real server ticks.
- AC-4 PASS: live PRE/POST events were observed before removal; `UNLOADED_TO_CHUNK` removal disposed and unregistered the runtime, repeated detach/remove stayed idempotent, and counts remained unchanged over later server ticks.
- AC-5 PASS: save/remove/load created fresh runtime, context, and controller identities; `worldData()` matched each held runtime; active mining, goal, path, and in-progress calculation state were absent after reload.
- Controller protocol PASS: own-container swap, foreign-container rejection, invalid `-1`/`9` rejection, selected-hand synchronization, survival mode, rejected interactions, and unchanged terrain were exercised.

Checks actually run:

- `.\\gradlew.bat :worker:runGameTestServer :worker:checkstyleGameTest --no-daemon --console=plain` — exit 0; 7 required GameTests passed (5 M2.2 tests plus 2 existing worker host tests), Checkstyle passed. Raw transcript: `logs/focused-worker-runtime-final.log`.
- `.\\gradlew.bat :worker:runGameTestServer :worker:sensorTest :worker:checkstyleGameTest --continue --no-daemon --console=plain` — exit 0 before the final test-only assertion cleanup; GameTests, sensor tests, and GameTest Checkstyle passed. The architecture sensor result is retained in `sensor-architecture-test.xml`.
- Final GameTest compilation emitted 12 nonblocking Error Prone `ReferenceEquality` warnings. These are intentional identity assertions required by the ownership criteria; no unused-variable warning remains.
- Luna executed the parent-selected `.\\gradlew.bat :worker:spotbugsMain :worker:checkstyleMain --continue --no-daemon --console=plain` — exit 1: `checkstyleMain` passed, while SpotBugs reported exactly two `EI_EXPOSE_REP` findings in `WorkerContext.player()` and `WorkerContext.playerController()`. Raw analyzer XML: `spotbugs-main-raw.xml`.

Repair note: the first removal test version was vacuous because event collection remained disabled before removal. It was corrected to collect live events, require nonzero PRE/POST counts, then retain collection through unload and assert no later events.

PENDING: full M2 gate, fresh-context independent milestone verification, and independent confirmation of the two human-approved exceptions.

## Original findings and decision rationale

The existing approved policy permits exceptions only for findings present at e81
and states: "Findings added or changed by subsequent implementation are new and
blocking; they cannot inherit an exception just because a similar warning existed."
See [the approved warning policy](PROPOSED-M2-entry-warning-policy.md), rules 1–5.
Before human approval, no exception, suppression, analyzer filter or policy change had been applied to
the candidate. M2.2 was blocked despite passing behavior tests.

Both new findings are `EI_EXPOSE_REP` in
`worker/src/main/java/automatone/worker/WorkerContext.java`:

| Method/signature | Line | Required contract |
|---|---:|---|
| `player()Lnet/minecraft/world/entity/LivingEntity;` | 25 | Return the actual worker supplied to this runtime; copies would not be its server host. |
| `playerController()Lbaritone/api/utils/IPlayerController;` | 45 | Return the one stable controller bound to that worker; detached replacements would change explicit controller identity. |

The approved M2 implementation plan requires these live identities. Focused
ownership/controller GameTests exercise the identities and route inventory
changes to the actual worker; the analyzer report still correctly remains FAIL.
Unlike M2.1's encapsulation repair, substituting a copy here would contradict the
public host contract. The assumption that this adapter could pass the unchanged
new-warning policy without an explicit exception is disproven.

WorkerContext canonical UTF-8/LF SHA-256:
`7cfc075211e43f866cd5dfd10bb04be0a7108e62420b4b39070b300afb114eb3`.
Unsuppressed analyzer XML byte SHA-256:
`20403e89c297b742731f6d09c7e4662ba94459218308f73341b5742fafa52448`.
The report analyzed all four worker classes and contains exactly two findings,
zero analyzer errors and zero missing classes. Complete identities are retained
in `ownership-findings.json`; both were initially BLOCKING_NEW_FINDING with null approval and now record the explicit human decision.

Proposed human decision: approve only these two exact live-identity exceptions
for this source, with independent confirmation at the single final M2 gate.
Any implementation would retain the unsuppressed report and enforce exact source
and finding identities; additional or changed warnings would remain blocking.
This would allow M2.2 completion and continuation to M2.3 while preserving the
required final milestone verification. It is not a blanket expansion of warning
eligibility or approval of the remaining M2 work without its tests.

Controller confirms focused AC-1–AC-5 and the controller protocol have measured
passing evidence after the test corrections. Native tick/pathing ownership,
server authority and the API boundary remain intact. Milestone acceptance is withheld pending the final independent gate. Wider M2 checks, including
dependency verification and the final clean independent profile, remain PENDING.
Graphify incremental update passed after the lifecycle changes: 4,617 nodes, 12,129 edges. Seven known partial Groovy parses remain advisory limitations.

## Human-approved exceptions

On 2026-09-05 the user answered "yes" to approval of exactly the two exceptions
above, subject to independent confirmation at the final M2 gate. This resolves
the human decision blocker. Implementation of the exact gate and its focused
controls is in progress; M2.2 completion awaits those controls. The seven passing
GameTests and four architecture checks remain applicable: no Java source changed.

The gate reuses existing unsuppressed analysis and exact matching with a separate,
frozen two-entry worker inventory and source hash. It records human approval,
not an independent review that has yet to happen. The library inventory is unchanged.

## Controller completion after approval

The focused command
`./gradlew.bat verifySpotBugsDispositionsContract :worker:spotbugsMain --init-script scripts/workflow/VerifySpotBugsDispositions.Tests.gradle --no-daemon --console=plain`
passed in 22 seconds. It exercises both existing library controls and the new
worker positive/negative controls, retains fresh unsuppressed analysis, and runs
the worker main analyzer behind the exact gate. Raw analysis contains precisely
the two approved findings, zero errors, and zero missing classes. See
`.agents/evidence/M2.2/logs/approved-exception-gate.log` and
`.agents/evidence/M2.2/spotbugs-main-approved-raw.xml`.

The test harness required two corrections: create temporary manifest directories,
and inspect declared task dependencies without resolving cross-project leases
inside an executing parallel task. Initial attempts failed/hung; they were not
counted as passing. Diagnostic notes are retained in
`logs/approved-exception-gate-harness-repairs.md`; the successful transcript above
is the final measurement.

Controller confirms M2.2 COMPLETE: AC-1 through AC-5 and controller protocol retain
the earlier measured passing evidence, Java source is unchanged, and the exact
exceptions now have explicit human authority and passing gate controls. M2.3 may
continue. Full M2 profiles, including dependency checks and independent technical
confirmation of these two exceptions, remain PENDING. No broad suite was repeated.